node_modules/
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# PocketBase lokale Testinstanzen / Binaries (nicht Teil des Repos)
pocketbase/pb_data/
pocketbase/*.zip
pocketbase/pocketbase
pb_data/
*.db
*.db-shm
*.db-wal

# Persönliche Daten — NICHT ins öffentliche Repo
# (Auftrittstermine sind Norberts Kalender; das Repo BenditoT/sabbatjahr ist public)
termine-events.json
Termine.csv

# Betriebssystem / Editor
.DS_Store
*.log

# Playwright-Browser (lokal, nicht ins Repo)
.pw-browsers/
